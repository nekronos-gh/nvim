return {
    'neovim/nvim-lspconfig', 		        -- LSP configuration
    'williamboman/mason.nvim',		        -- LSP, DAP, linter manager
    'williamboman/mason-lspconfig.nvim',	-- Mason suport for LSP config
    'ErichDonGubler/lsp_lines.nvim',	    -- LSP diagnostics in separate lines
}
